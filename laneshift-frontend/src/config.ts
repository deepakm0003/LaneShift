/**
 * LaneShift Frontend Config
 * =========================
 * Change API_BASE to point at a remote server when deploying.
 * Default: local FastAPI instance.
 */
export const API_BASE = 'http://localhost:8000'
